1. 创建 certs 目录（如果不存在）
2. 进入 certs 目录
3. 生成 CA 根证书（ca.key 和 ca.crt）
4. 生成 api.openai.com 域名证书（api.openai.com.key、api.openai.com.csr 和 api.openai.com.crt）
使用方法:
您可以双击运行这个批处理文件，或者在命令提示符中执行它。脚本会在完成后显示生成的文件列表。